export class Food {
  name: string;
  calories: number;
  protein: number;

  constructor(name: string, calories: number, protein: number) {
    this.name = name;
    this.calories = calories;
    this.protein = protein;
  }
}
